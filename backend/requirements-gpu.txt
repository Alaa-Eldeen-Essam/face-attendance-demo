# GPU-optimized requirements for CUDA 12.x
# Use this instead of requirements.txt when building GPU Docker images
# Install with: pip install -r requirements-gpu.txt

fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
pgvector==0.2.4

# InsightFace for face recognition
insightface==0.7.3

# ONNX Runtime with GPU acceleration (CUDA 12.x)
# Provides ~10x faster inference than CPU
onnxruntime-gpu==1.16.3

# Computer Vision
opencv-python==4.8.1.78
numpy==1.24.3
Pillow==10.1.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
requests==2.31.0
